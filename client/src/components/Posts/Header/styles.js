import { makeStyles } from "@material-ui/core";

export default makeStyles(() => ({
    header: {
        backgroundColor: '#FFE194',
        borderRadius: '20',
        margin: '4.15 vh',
        display: 'flex',
    }
}))