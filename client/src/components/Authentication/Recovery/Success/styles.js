import { makeStyles } from '@material-ui/core';

export default makeStyles(() => ({
    notification: {
        textAlign: 'center',
    },
    successText: {
        marginTop: '40px',
        fontFamily: 'sans-serif',
        fontSize: '1.6rem',
        fontWeight: '600',
        padding: '10px',
    },
}));
