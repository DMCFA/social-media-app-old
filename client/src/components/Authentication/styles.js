import { makeStyles } from "@material-ui/core";

export default makeStyles(() => ({
    body: {
        minHeight: '100%',
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center'
    },
    bnt: {
        display: 'flex',
        
    }
}))