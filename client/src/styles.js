import { makeStyles } from "@material-ui/core";

export default makeStyles(() => ({
    image: {
        minHeight: '100%',
        minWidth: '1024px',
        width: '100%',
        height: 'auto',
        position: 'fixed',
        top: 0,
        left: 0
    }
}))